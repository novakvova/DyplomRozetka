# PostgreSQL

Backend uses Entity Framework Core with PostgreSQL and creates the database on first start.

Database name:

```text
rozetka_unified
```

Connection string:

```text
Host=localhost;Port=5432;Database=rozetka_unified;Username=postgres;Password=postgres
```

The manual SQL review schema is stored in `database/schema.sql`.
